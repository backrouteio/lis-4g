# LIS Standalone — India 4G LTE
# Core (required)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Database (optional — only for PostgreSQL)
psycopg2-binary>=2.9.9

# SFTP for HI3 CC delivery (LEA receiver)
paramiko>=3.4.0

# ASN.1 encoding/decoding (X2 IRI — Python-based)
pyasn1>=0.6.0

# HTTP client (HI2 push delivery)
httpx>=0.27.0

# Crypto (TLS, key generation)
cryptography>=42.0.7

# Install command (Ubuntu / Debian):
# pip3 install fastapi uvicorn psycopg2-binary paramiko pyasn1 httpx cryptography --break-system-packages
